name: Expressions

on:
  push:

jobs:
  sample1:
    name: sample 1
    runs-on: ubuntu-latest
    steps:
      - name: contains true
        if: contains(fromJSON('["one", "two"]'), 'one')
        run: echo "true"
      - name: contains false
        if: contains(["one", "two"], 'three')
        run: echo "false"
