name: Expressions

on:
  push:

jobs:
  sample1:
    name: sample 1
    runs-on: ubuntu-latest
    steps:
      - name: contains true
        if: contains(fromJSON('["one", "two"]'), 'one')
        run: echo "true"
      - name: format
        if: format('Hello {0} {1}', 'Tom', 'World') == 'Hello Tom World'
        run: echo 'Hello Tom World'
      - name: format2
        run: format('Hello {0} {1}', 'Tom', 'World') == 'Hello Tom World' && echo 'Hello World' || 'Not'
